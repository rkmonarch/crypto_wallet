'use strict';

var streams = require('@moralisweb3/streams');



Object.keys(streams).forEach(function (k) {
	if (k !== 'default' && !exports.hasOwnProperty(k)) Object.defineProperty(exports, k, {
		enumerable: true,
		get: function () { return streams[k]; }
	});
});
